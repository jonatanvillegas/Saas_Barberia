# Frontend (Vite)
# Si el backend vive en otro dominio, ponlo aquí.
# Ojo: el código agrega automáticamente /api si no lo pones.
# Ejemplo: https://tu-backend.com  -> usará https://tu-backend.com/api
VITE_API_URL=http://localhost:5000
